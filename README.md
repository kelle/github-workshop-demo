# GitHub Workshop Demo

This is a tiny Python demo repository for a GitHub workshop.

## Files
- `calculator.py` — defines a simple math helper
- `main.py` — runs the demo from the command line

Run it with:

```bash
python3 main.py
```
